{
  "name": "elementfi-rabbithole",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ elementfi-rabbithole",
    "create-local": "graph create --node http://localhost:8020/ elementfi-rabbithole",
    "remove-local": "graph remove --node http://localhost:8020/ elementfi-rabbithole",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 elementfi-rabbithole"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.1",
    "@graphprotocol/graph-ts": "^0.23.1"
  }
}
